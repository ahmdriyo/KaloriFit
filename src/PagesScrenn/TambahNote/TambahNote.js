import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const TambahNote = ({ navigation }) => {

  navigation.navigate('Home')
  return (
    <View>
      <Text>TambahNote</Text>
    </View>
  )
}

export default TambahNote

const styles = StyleSheet.create({})